spring:
  application:
    name: booking-service
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:

  sql:
    init:
      mode: always

server:
  port: 8204
  error:
    include-message: always